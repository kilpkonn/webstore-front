stages:
  - build
  - deploy

build webstore:
  stage: build
  image: node:11-alpine
  cache:
    paths:
      - node_modules
  artifacts:
    paths:
      - dist
  tags:
    - webstore
  variables:
    api: localhost/api
  script:
    - yarn install
    - yarn run build

deploy webstore:
  stage: deploy
  tags:
    - webstore
  script:
    - mkdir -p ~/front-deployment
    - rm -rf ~/front-deployment/*
    - cp -r dist/frontend/. ~/front-deployment
